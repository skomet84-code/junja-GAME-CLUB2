# JUNJA GAME CLUB v1.5 TEST REPORT

## 적용 항목
- 경마 1~7번 말 원터치 카드 선택 UI
- 단승은 1마리, 복승/쌍승은 2마리 선택 완료 전 START 비활성화
- 경마 종료 후 선택마별 최종 순위, 베팅액, 지급액, 손익을 고정 결과판에 표시
- 결과판은 자동으로 사라지지 않으며 `다음 경주표 받기`를 사용자가 직접 누르는 구조
- 경마 화면을 나갔다 다시 들어와도 현재 세션의 마지막 결과판 유지
- 섯다/맞고 1만·2.5만·5만·10만(MAX) 원터치 판돈 버튼
- 섯다/맞고 직접 입력도 유지하며 1,000G 단위 및 MAX 100,000G 보정
- 모바일 숫자 입력 스피너 제거 스타일 추가
- 기존 서버 정산/Neon 영구저장 로직 미변경

## 검사
- `node --check public/app.js` 통과
- `node --check server.js` 통과
- v1.5 PWA 캐시 키 및 정적 파일 버전 쿼리 갱신
- 경마 API 단승/복승/쌍승 서버 입력 검증 로직 유지
- 섯다/맞고 서버 판돈 MAX 100,000G 검증 로직 유지

## 로컬 API 통합 테스트
- 신규 회원 생성 후 경마 경주표 API 정상
- 단승 10,000G 경주 실행 HTTP 200 및 순위/지급 결과 정상
- 쌍승 동일 말 중복 선택 서버 차단 HTTP 400
- 쌍승 서로 다른 말 2마리 정상 실행 HTTP 200
- 섯다 100,000G 정상 시작 HTTP 200
- 섯다 101,000G 초과 서버 차단 HTTP 400
- 맞고 101,000G 초과 서버 차단 HTTP 400
