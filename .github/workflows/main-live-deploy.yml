name: JUNJA LAND Cost Lock

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cost-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Cost lock status
        run: |
          echo "Automatic GitHub Actions verification/deploy is disabled."
          echo "Render auto-deploy is managed separately."
          echo "Neon persistence is disabled in application code."
